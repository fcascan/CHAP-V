# Core (GPU mode needs OpenCL support — PyPI opencv-python wheels include it)
opencv-python>=4.8.0
numpy>=1.24.0,<2.0.0
psutil>=5.9.0
pyudev>=0.24.0
Pillow>=9.0.0

# Web interface
Flask>=2.3.0
Flask-SocketIO>=5.3.0
eventlet>=0.33.0

# NPU inference (rknnlite is installed from a local wheel)
# rknn_toolkit_lite2>=2.3.0

# CPU inference
onnxruntime>=1.15.0
